def simplify_text(text: str) -> str:
    """
    Dummy simplification logic.
    Replace with ML/AI simplification pipeline.
    """
    return "Simplified: " + text[:200] + ("..." if len(text) > 200 else "")
